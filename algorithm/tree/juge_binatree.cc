//查找二叉树（BST）
//一棵二叉搜索树（Binary Sort
// Tree）是以一棵二叉树来组织的，可以用链表数据结构来表示，其中，每一个结点就是一个对象，一般地，包含数据值和指向孩子（也可能是父母）的指针。
//如果某个孩子结点不存在，其指针为空（NULL）。
//查找树的左右子树各是一棵查找树
//若查找树的左子树非空，则其左子树上的各节点值均小于根节点的值。
//若查找树的右子树非空，则其右子树上的各节点值均大于根节点的值。
//面试官还要问能不能用迭代写出来
